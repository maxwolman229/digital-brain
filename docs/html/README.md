# Danieli HTML Outputs

Archived Claude Code HTML outputs for the temporary Danieli client share.

These files are kept for review history only. They are not served directly from Vercel, and the deployed share uses the server-side password gate in `apps/marketing/src/pages/danieli/`.

The standalone access page is historical reference material and does not contain the deployed shared password.
